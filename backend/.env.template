# Gamora X: Backend Configuration Template
# Instructions:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key" to download the JSON
# 3. Copy the ENTIRE JSON content and paste it as the value for FIREBASE_SERVICE_ACCOUNT
# 4. Rename this file from .env.template to .env

PORT=5000
FRONTEND_URL=http://localhost:5173

# Paste the full JSON string below (ensure it's all on one line or properly quoted)
# Example: FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'
FIREBASE_SERVICE_ACCOUNT=
